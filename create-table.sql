CREATE TABLE cities (
id INT,
city VARCHAR(99),
state_id CHAR(2),
county VARCHAR(99),
latitude DECIMAL(7,4),
longitude DECIMAL(7,4),
population INT,
incorporated CHAR(5),
timezone VARCHAR(99)
);
